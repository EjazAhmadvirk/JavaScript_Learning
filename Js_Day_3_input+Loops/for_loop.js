for(let i = 0; i < 5; i++) {
    console.log(`Current value of i: ${i}`);
}   

// in this loop variable initialized + checked + incremented in one line
// This code initializes a variable i to 0 and uses a for loop to print the current